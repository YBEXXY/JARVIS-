# Core runtime dependencies
numpy>=1.26,<3
opencv-python>=4.10
SpeechRecognition>=3.10
pyttsx3>=2.90
Pillow>=10
PyQt5>=5.15.9; python_version < "3.13"
python-dotenv>=1.0
requests>=2.31
psutil>=5.9
pywin32>=306; platform_system == "Windows"

# Optional microphone backend for SpeechRecognition on Windows.
# This package is intentionally constrained because wheels may not exist yet for newest Python releases.
PyAudio>=0.2.13; platform_system == "Windows" and python_version < "3.13"
